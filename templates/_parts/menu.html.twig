<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        {% set items = getMenu() %}
        <li class="nav-item">
          <a class="nav-link {% if item == 'Accueil' %}active{% endif %} " href="{{path('app_front')}}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {% if item == 'Catégorie' %}active{% endif %} " href="{{path('app_list_categories')}}">Catégorie</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {% if item == 'Contact' %}active{% endif %}" href="{{path('app_static_page',{'page':'contact'})}}">Contact</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Pages</a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="{{path('app_static_page',{'page':'presentation'})}}">Présentation</a>
            <a class="dropdown-item" href="{{path('app_static_page',{'page':'mentionsLegal'})}}">Mentions Légales</a>
          </div>
        </li>
      </ul>
      <form class="d-flex">
        <input class="form-control me-sm-2" type="search" placeholder="Search">
        <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>